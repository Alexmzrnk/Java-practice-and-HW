package class8;

public class E5WhileAndBreak {

    public static void main(String[] args) {

        /*creat a boolean variable summer store true in it then write a loop
        that runs as long as it is summer create a temp variable store 85
        inside the loop check the temp it it is less than 100 print "its good I enjoy summer"
        asa temperature exceeds 100 print "it's very hot" and break the loop with break keyword
         */
        boolean summer=true;
        int temp=103;
        while (summer){
            if (temp<100){
                System.out.println("It's good I enjoy summer");
            } else {
                System.out.println("It's very hot");
            }break;
        }

    }
}
